var app = angular.module('brand_app', ['pagination']);

var specification_app = angular.module('specification_app', ['pagination']);

var content_app = angular.module('content_app', ['pagination']);

var typeTemplate_app = angular.module('typeTemplate_app', ['pagination']);

var goods_app = angular.module('goods_app', ['pagination']);